1. boolem, string, integer, undefined, null

Я пока не знаю

2. = - для присваивания
   == - для сравнения не строгого даже если разный тип данных будет true
   === - строгое сравнение где и тип данных учитываются 

3. для своего рода задач, каждый обладает своими возможностями  
   return 
   continue


4. есть переменные глобальные есть локальные 
   локальные они находятся например в фунции их там создали 
   и они пренадлежат только этой функции вне её эта переменная не существует 
   глобальные же используются везде не только в одной фунцкии она доступна всем


5. когда написан оператор инициализации let/fucntion или другие
   после написания =
   будет undefined

6. метод это действие какое-то создать что-то удалить 
   свойство это описание какого-то объекта

7. let a = 23
   
   bigint
   Infinity - (бесконечность) 
   - Infinity - (-бесконечность)
   1e8 - е умножает на 1 указанным количеством нулей (8)
   или указания в системах счисления 
   BigInt числа дают возможность работать с целыми числами произвольной длины
   Они нужны достаточно редко и используются в случаях 
   когда необходимо работать со значениями более чем 253 или менее чем -253


Как создать новую переменную и записать туда число? 
Какие группы значений для чисел существуют?
Какие специальные значения для чисел существуют? 
Как получить эти специальные значения?
Как эти специальные значения ведут себя в математических операциях?

8. +,-,*,/,++,--,% 
   Math это объект хранящий в себе математические операции и константы   
   Какие математические операции вы знаете? Что такое Math?


9. parseInt("берёты параметром строку где лежит число в какой-нибудь системесчисления", число обозначающие в какой системе счсления разобрать строку)
   parseFloat(принимает строку и возвращает число с плавающей точкой)
   Расскажите про parseInt,parseFloat? Для чего они нужны и как работают?



10. Math.ceil(округляет вверх)
    Math.floor(округляет вниз)
    math.round(округляет к ближайшему целому)

    3.43242432.tiFixed(3) -> 3.432 правда вернёт строку но можно обернуть это действие в Number 
    либо parseFloat или же после действия /1 
    Как округлить число до целого? Как округлить число до 3 знака после запятой? Как округлить до ближайшего целочисленного вверх и вниз ?


11. let a = "dsa" 
 Как создать новую переменную с каким-нибудь строковым литералом?


12. a[2] 
    Как вывести 3й символ в этой строке?


13. да 
    можно при помощи replace(/какой симовл хотим изменить/ , "на что поменять" ) 
    Можем ли мы заменить этот символ? Если да, то как?
14. String(а)
    когда выполняется какое-то действие например функция toFixed() принимает число возврощает строку
    Как явно преобразовать переменную к строке?Когда происходит неявное преобразование к строке?



15. a.length
    Как посмотреть длину строки?


16. let a = "borderRadiusooo"

    for(let i =0; i <= a.length; i++ ) {
       a[i] === "o" ? console.log("bingo"): null
    }
    нужно использовать if() 
    Как проверить есть ли в строке какое-нибудь слово (это выражение нужно будет использовать внутри if(...) )?


17. a.substring(index)
    Как взять подстроку от строки?



18. a.toUpperCase() 
    a.toLowerCase()    
    Как привести строку к верхнему регистру, а потом - к нижнему?


19. a.split("")
    Как преобразовать строку в массив символов?


20. let a = true or false 
    Boolean(2) 
    [1,2,3] == [1,2,3] 
    Как создать новую логическую переменную? Как явно преобразовать переменную к логическому виду? Когда происходит неявное преобразование?



21. true or false
    К каким логическим значениям преобразуются все другие типы?



22. && || ! 
    Какие логические операторы вы знаете и как они работают (напишите таблицы истинности)?


23. undefined - неизивестно
    null - ничего
    при сравнении получаем false 
    если undefined === undefined (будет true с null тоже)
    undefined == null (true)
    undefined === null (false)
    спокойно переобразуются в другие типы
    Что такое undefined? Что такое null? Как эти типы ведут себя при преобразованию к другим типам и при сравнении с другими типами ?


24. объект это - хранилище где ты записываешь свойства или методы 
    в свои index 
    let a = {}
    a.name = "Vlad"
    a.name = "alex"
    delete(a.name)
    a.name_friend = "Bob" 
    
 Что такое объект?
 Как создать новый объект?
 Как добавить свойство в объект?
 Как поменять свойство в объекте?
 Как удалить свойство из объекта?
 Как задать свойство в названии которого есть пробел?



25. это когда у тебя не сами данные а ссылка на массив или объект где они хранятся 
    примитив это те данные у которых есть имя и данные внутри него в виде строки или числа 
    let a = object
    let b = {...a}
    сравниваются по свойствам 
    Что такое ссылочный тип данных? В чем отличие от примитивов? Как копируются объекты? Как склонировать объект? Как сравниваются объекты?



26. это хранилище данных где index от 0 
    и его нельзя изменить 
    let a = []
    массив может содержать элементы разных типов
    Что такое массив? Как создать новый пустой массив? Может ли массив содержать элементы разных типов?


27.  array.push(elem)
     arrat.unshift(elem)
     
     быстрее работает push  по скольку ему нужно добавить элемент в конце 
     а unshift сдвинуть элементы а потом добавить 

     
     Как добавить новый элемент в конец массива или в его начало? Что работает быстрее?



28. array.splice(-1)
    array.splice(0, 1) 
    или shift()    
    Как убрать элемент из конца массива или из его начала?


29.array.length
    Как посмотреть длину массива?


30. let obj2 = ["3123",{a: 1, c: 3}, "555", "43243", "dsadas", {a: 1, c: 3}]

    for(let i = 0; i <= obj2.length; i++) {
        typeof(obj2[i]) === typeof({}) ? console.log(i): null
        obj2[i] === "555" ? console.log(i): null
    } 
    Как проверить на каком индексе в массиве находится строка ‘555’ ? А как проверить на каком индексе находится объект {a:1} ? Как проверить если ли указанные выше элементы в массиве?


31. array.toString(",") 
    Как из массива слов получить строку из слов, разделенных запятыми?


32. let obj2 = ["3123", "555", "43243", "dsadas", "555"]

    for(let i = 0; i <= obj2.length; i++) {
       obj2[i] === "555"  ? obj2.splice(i, 1) : console.log("not good")
    }
    Как удалить из массива элемент-строку “555” ?



33. toString() - делает из массива строку
    map(func(elemAr,  index, massiv)) - преобразует элементы массива потом возвращает их
    splice(index, сколько элементов удалить)- для удаления элементов массива 
    forEach(func(elem, index,massiv)) - позволяет запускать функцию для каждого элемента
    filter(function(item, index, array) - возвращает массив из всех подходящих элементов
    revese() - переворачивает массив  
    split("") - из строки делает массив ("")для деления строки на буквы где у каждой буквы свой индекс
    Какие методы преобразования массивов вы знаете? Какой у них синтаксис и для чего они нужны?


34. важен потому что у каждый функции прописана логика действий на аргумент 
    и так же прописано какой дожен быть аргумент строка или число или массив ...
    аргумент это данные указываемые в скобках при вызове функции 

 Важен ли для функции порядок аргументов? Что такое arguments?



35. let a = new Date() 
    let date = new Date(3600 * 440 * 1000000);

 Как создать новую дату? А как получить ее сразу в миллисекундах?


36. setTimeOut(function, time) 
    Как вызвать какую-нибудь функцию через 10,5 секунд ?

37. JSON это формат для обмена и хранения данных 
    JSON.parse(из строки делает объект)
    JSON.stringify(из объекта делает строку) 
Что такое JSON ? Какие методы для работы с JSON вы знаете?


38. Как в javascript обрабатывать ошибки?
    c помощью консоли, точек остановки, и сторонних инструментов 

39. Какие браузерные события вы знаете?
    onMouse, onContextMenu, focus, onclick
    

40. Как предотвратить запуск стандартного обработчика события в браузере?
    в обработчике написать 
    > event.preventDefault()
    > return false

41. Что такое DOM-дерево и как с ним работать? Например, как в него вставить новый элемент?
    DOM-дерево - это интерфейс дающий доступ к HTML XHTML XML документам
    let div = document.createlemt("tagName")
    div.innerHTML="text or tag"    
    document.body.append(div)

42. Что такое window, location , document ?
    window - это окно содержащий  DOM-документ так же он 
    содержит описание всех методов 
    свойств и событий доступных через объект window
    
    location - это объект в котором находятся данные об урле 
    то есть где мы сейчас на каком сайте в каком разделе 
    какой протокол используется 

    document - это HTML страницы 

